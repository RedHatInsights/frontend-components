export type GroupType = 'treeView' | 'checkbox' | 'radio' | 'button' | 'plain';

const groupType = {
  treeView: 'treeView',
  checkbox: 'checkbox',
  radio: 'radio',
  button: 'button',
  plain: 'plain',
};

export default groupType;
