import React from 'react';
import { Text, TextVariants } from '@patternfly/react-core';

export const DEFAULT_MODAL_TITLE = 'Manage columns';
export const DEFAULT_MODAL_DESCRIPTION = <Text component={TextVariants.p}>Selected categories will be displayed in the table.</Text>;
