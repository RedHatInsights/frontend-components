language: node_js
sudo: required
cache:
  directories:
  - "$HOME/.npm"
notifications:
  email: false
node_js:
- '10'
install:
  - npm ci
script:
  - npm run lint
  - npm test
  - npm run coverage
after_success:
  - cp .npmrc.template $HOME/.npmrc
